<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ProyectoTicketing.Vistas.VentanaGeneral_Ver_Tickets"
             Title="VentanaGeneral_Ver_Tickets">

    <ScrollView>
        <StackLayout Padding="10">
            <Label Text="Tickets" FontSize="24" HorizontalOptions="Center" Margin="0,10"/>

            <CollectionView x:Name="TicketsCollectionView" 
                        SelectionMode="Single" 
                        ItemsSource="{Binding Tickets}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Padding="10" Margin="5" BorderColor="Gray" CornerRadius="5">
                            <HorizontalStackLayout Spacing="10">
                                <Label Text="{Binding User}" FontAttributes="Bold" VerticalOptions="Center"/>
                                <Label Text="{Binding TicketDescription}" LineBreakMode="TailTruncation" VerticalOptions="Center"/>
                                <Label Text="{Binding TicketType}" FontSize="Small" TextColor="Gray" VerticalOptions="Center"/>
                                <Label Text="{Binding Status}" FontSize="Small" TextColor="Orange" VerticalOptions="Center"/>
                                <Label Text="{Binding Importance}" FontSize="Small" TextColor="Red" VerticalOptions="Center"/>
                                <Label Text="{Binding LastUpdated}" FontSize="Small" TextColor="Gray" VerticalOptions="Center"/>
                            </HorizontalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </StackLayout>
    </ScrollView>
</ContentPage>